<h1>New Firmware</h1>

<%= form_for([:switch_model, @firmware]) do |f| %>
  <% if @firmware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@firmware.errors.count, "error") %> prohibited this firmware from being saved:</h2>

      <ul>
      <% @firmware.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', switch_models_path %>
