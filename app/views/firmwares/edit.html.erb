<h1>Editing Firmware</h1>
<%= bootstrap_form_for @subject, url: "/switch_models/#{@switch_model.id}/firmwares/#{@subject.id}", method: "put"  do |f| %>
  <% if @subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject.errors.count, "error") %> prohibited this firmware from being saved:</h2>

      <ul>
      <% @subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field stand_width">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit :class =>"btn btn-primary "  %>
    <%= link_to 'Back',switch_models_path, :class =>"btn btn-primary " %>
  </div>
<% end %>


