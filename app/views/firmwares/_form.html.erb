<%= form_for(@firmware) do |f| %>
  <% if @firmware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@firmware.errors.count, "error") %> prohibited this firmware from being saved:</h2>

      <ul>
      <% @firmware.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :switch_model_id %><br>
    <%= f.number_field :switch_model_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
