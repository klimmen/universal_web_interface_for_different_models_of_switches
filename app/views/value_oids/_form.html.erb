<%= form_for(@value_oid) do |f| %>
  <% if @value_oid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@value_oid.errors.count, "error") %> prohibited this value_oid from being saved:</h2>

      <ul>
      <% @value_oid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
